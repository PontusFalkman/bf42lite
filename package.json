{
  "name": "bf42lite",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:client": "pnpm --filter @bf42lite/client dev",
    "dev:host": "echo \"host-node not implemented yet\"",
    "build": "pnpm -r run build",
    "clean": "pnpm -r run clean",
    "prebuild": "node tools/check-workspace-deps.cjs"
  },

  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.0"
  }
}
