{
  "name": "bf42lite",
  "private": true,
  "packageManager": "pnpm@10.20.0",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @app/client-tauri dev",
    "lint": "eslint . --ext .ts",
    "clean": "rimraf **/dist"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.6.0"
  }
}
