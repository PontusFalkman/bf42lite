{
  "name": "bf42lite",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:client": "pnpm --filter @app/client-tauri dev:tauri",
    "dev:host": "pnpm --filter @host/node dev",
    "build": "pnpm -r run build"
  },
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}